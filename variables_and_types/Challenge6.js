var prompt = require('prompt-sync')();

let a =parseFloat(prompt("entre le nombre a:"));

let b=parseFloat(prompt("entre le nombre b:"));


console.log(`a+b=${a+b}, a-b=${a-b},a*b=${a*b},a/b=${a/b}`);
